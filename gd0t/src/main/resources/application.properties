spring.application.name=gd0t

# Database Connection (Reads Render env vars, falls back to local for testing)
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/devblog}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:po0+89ka5-0Kalk}

# Hibernate / JPA Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true

# Custom Admin Password for your Seeder
blog.admin.password=${ADMIN_PASSWORD:devpassword}